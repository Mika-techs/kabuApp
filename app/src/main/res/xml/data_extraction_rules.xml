<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device. The users table holds Keystore-encrypted credentials, which are
  undecryptable elsewhere anyway, and everything else re-fetches from digikabu.de in one call.
  android:allowBackup is ignored from Android 12, so these rules are what actually apply there.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
